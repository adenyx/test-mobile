import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    padding: 8,
    borderWidth: 1,
    borderRadius: 12,
    borderColor: '#aaaaaa',
    backgroundColor: '#ffffff',
  },
  imageContainer: {
    width: 150,
    height: 85,
  },
  image: {
    width: '100%',
    height: '100%',
    borderRadius: 8,
  },
})
